# 서비스 기획서 — 붉은 서재의 비밀

## 1. 서비스 목적

- 직접 만든 살인 사건 시나리오를 바탕으로, 이용자가 **AI 용의자를 심문**하고 **AI 수사 지휘관에게
  채점**받는 몰입형 추리 웹게임을 제공한다.
- 정적인 텍스트 추리물과 달리, 매 질문마다 AI가 실시간으로 다른 답을 생성하므로 플레이할 때마다
  대화의 흐름이 달라지는 재플레이 가치를 준다.

## 2. 타겟 사용자

- 추리/미스터리 장르를 좋아하는 캐주얼 게이머
- 짧은 시간(10~15분) 안에 몰입할 수 있는 웹 기반 미니게임을 찾는 모바일 사용자
- PC/모바일 구분 없이 접근하는 일반 방문자 (반응형 필수)

## 3. 페이지 구성 및 메뉴 이동

상단 고정 내비게이션 바(모바일은 햄버거 메뉴)로 아래 3개 섹션을 자유롭게 이동한다.

1. **사건 개요 (`index.html`)** — 사건 브리핑, 진행 3단계 안내, 증거 미리보기, "수사 시작" CTA
2. **사건 현장 · 심문 (`case.html`)** — 증거 5종 상세 모달, 용의자 4명 프로필 + AI 심문 채팅창
3. **최종 추리 제출 (`verdict.html`)** — 범인/흉기/동기 선택 폼 + AI 채점 결과 패널

## 4. 핵심 기능

| 기능 | 설명 |
|---|---|
| 증거 상세 보기 | 카드를 클릭하면 감식 결과 상세 정보를 모달로 표시 |
| AI 용의자 심문 | 용의자 선택 후 자유 질문 입력 → AI가 해당 인물로 실시간 답변 (핵심 AI 기능 ①) |
| 대화 맥락 유지 | 용의자별로 최근 대화 이력을 유지해 이어지는 질문에 대응 |
| 최종 추리 제출 및 AI 채점 | 범인/흉기/동기 선택 + 추리 근거 작성 → AI가 정답 여부와 사건 전말을 코멘트 (핵심 AI 기능 ②) |
| 반응형 레이아웃 | 데스크톱/태블릿/모바일 3단 구간 대응, 모바일 햄버거 메뉴 |

## 5. AI 기능의 입력 / 출력 / 실패 처리 기준

### 기능 ① AI 용의자 심문 — `POST /api/interrogate`

- **입력**: 선택한 용의자 이름, 사용자가 입력한 질문(텍스트), 최근 대화 이력(최대 8턴)
- **출력**: 해당 용의자의 1인칭 답변 텍스트 (3~4문장 이내)
- **가치**: 플레이어가 스스로 질문을 설계하고 진술의 모순을 찾아내는 능동적 추리 경험 제공
- **실패 처리**
  - 빈 입력(필수값 누락): 질문 없이 전송 시 클라이언트에서 즉시 차단하고 안내 메시지 표시
  - API 오류(4xx/5xx): 서버가 502로 응답 시 "AI 서버와 통신 중 오류" 메시지를 채팅창에 표시
  - 지연/타임아웃: 서버는 12초, 클라이언트는 15초 기준으로 중단하고 재시도 안내 메시지 표시

### 기능 ② 최종 추리 판정 — `POST /api/verdict`

- **입력**: 범인/흉기/동기 선택값(라디오·셀렉트), 추리 근거(선택 입력, 최대 500자)
- **출력**: 정답 여부(`correct`), AI가 생성한 채점 코멘트 및 사건 전말 설명
- **가치**: 단순 정오답을 넘어, 플레이어의 추리 근거에 대한 개별 피드백을 받는 경험 제공
- **실패 처리**
  - 빈 입력(필수값 누락): 범인/흉기/동기 중 하나라도 비어 있으면 제출을 막고 안내
  - API 오류/타임아웃: AI 코멘트 생성이 실패해도 서버가 저장한 기본 판정 결과와 사건 전말은
    반드시 반환해, 사용자가 결과를 못 받는 상황을 만들지 않는다(우아한 실패 처리)

## 6. 향후 보너스 아이디어 (선택 과제 연계)

- 다크 모드 토글 (현재 다크 테마 고정 → 라이트 테마 대비 옵션 추가)
- 최종 추리 결과를 노코드 자동화(예: 이메일/스프레드시트)로 전송해 "정답률 통계" 수집
- 방문자 분석 스크립트 연동으로 어떤 용의자에게 질문이 몰리는지 측정
